APP_ENV=development
SECRET_KEY=replace-with-a-local-secret
ACCESS_TOKEN_EXPIRE_MINUTES=60
PASSWORD_HASH_ITERATIONS=100000

# Optional: enable real freedom-tag extraction via an OpenAI-compatible API.
# Leave these blank to keep the current fallback behavior {"keywords": []}.
LLM_PROVIDER=
LLM_API_KEY=
LLM_MODEL=
LLM_BASE_URL=
